object Build {
    object SigningConfig {
        const val debug = "debug"
        const val releaseAppBundle = "appbundle-release"
    }

    object BuildVariant {
        const val debug = "debug"
        const val release = "release"
    }
}